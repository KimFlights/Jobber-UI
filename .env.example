# Optional dev-proxy target overrides (see vite.config.ts). Defaults shown.
# Copy to `.env` and edit only if your backend runs on different hosts/ports.
VITE_GATEWAY_TARGET=http://localhost:8080
VITE_SCRAPER_TARGET=http://localhost:8082
